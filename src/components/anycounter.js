import React from "react"
import Button from '@material-ui/core/Button';

const AnyCounter = () => (
  <div>
    <div>
      Any-Counter
    </div>
    <Button variant="contained" color="primary">
      Hello World
    </Button>
  </div>
)

export default AnyCounter
